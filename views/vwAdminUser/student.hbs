<div class="card">
  <div class="card-header">
    Instructor
  </div>
    <div class="card-body">
    <h5>Profile</h5>
     <div class="form-group row">
        <label for="fullname" class="col-4 col-form-label ">Full Name: </label> 
        <div class="col-8">
          <p>{{this.student.FullName}}</p>
        </div>
      </div>
      <div class="form-group row">
          <label for="email" class="col-4 col-form-label ">Email:</label> 
          <div class="col-8">
            <p>{{this.student.Email}}</p>
          </div>
      </div>
      <table class="table table-bordered">
        <h5>Couses bought by {{this.student.FullName}}</h5>
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Created Time</th>
          </tr>
        </thead>
        <tbody>
          {{#if this.isEmpty}}
            <tr>
              <td colspan="3"><h4>Student has not purchased any courese</h4></td>
            </tr>
            
          {{else}}
            {{#each this.courses}}
            <tr>
              <th scope="col">{{ID}}</th>
              <td scope="col">{{Name}}</td>
              <td>{{formatDate CreatedTime}}</td>
            </tr>
          {{/each}}
          {{/if}}
        </tbody>
      </table>
</div>
  <div class="card-footer text-muted">
    <a name="" id="" class="btn btn-outline-success" href="/admin/user?role=2" role="button">
      <i class="fa fa-sign-out" aria-hidden="true"></i>
      Back</a>
</div>

